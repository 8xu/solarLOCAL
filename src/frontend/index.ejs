<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titlu %></title>
</head>
<body>
    <header>
        <p>Updated at <span id="time"></span></p>
    </header>

    <main>
        <section>
            <p>Production: <span><%= production %></span> W</p>
            <p>Consumption: <span><%= consumption %></span> W</p>
            <p>Grid: <span><%= grid %></span> W</p>
        </section>
    </main>

    <button onclick="window.location.reload()">Refresh</button>
</body>

<script>
    var date = new Date();
    if (date.getSeconds() < 10) {
        document.getElementById("time").innerHTML = date.getHours() + ":0" + date.getMinutes() + ":0" + date.getSeconds();
    }
    var time = date.getHours() + ":" + date.getMinutes() + ":" + date.getSeconds();
    document.getElementById("time").innerHTML = time;
</script>

<style>
    * {
        margin: 0;
        padding: 0;
    }

    body {
        font-family: monospace;
        color: #fff;
        background-color: #000;
        text-align: center;
        font-size: 2em;
        max-width: 100vw;
    }

    span {
        color: greenyellow;
    }

    main {
        padding: 2em;
    }

    main section {
        margin-bottom: 64px;
    }

    button {
        padding: 2em 4em;
    }
</style>
</html>